# 🚀 GitOps Kube Pipeline

CI/CD pipeline that builds, tests, and ships containerized apps straight to Kubernetes on every push. GitHub Actions handles the build and test stages while deployments follow a GitOps model, keeping every release declarative and easy to roll back. No manual `kubectl apply` at 2am — the pipeline handles it.
